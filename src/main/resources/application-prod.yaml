spring:
  config:
    activate:
      on-profile: prod

logging:
  config: classpath:logback-spring-prod.xml

datasource:
  driver-class-name: org.mariadb.jdbc.Driver
  url: jdbc:mariadb://database-1.c7iisia426xj.ap-northeast-2.rds.amazonaws.com:3306/CINEMA?useUnicode=true&characterEncoding=UTF-8

