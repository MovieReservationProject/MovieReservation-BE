server:
  port:8080

spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

#  autoconfigure:
#    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

  datasource:
    username: root
    password: nebyulla2024
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://database-1.cn6ioosku2ij.ap-northeast-2.rds.amazonaws.com:3306/movie_project?allowPublicKeyRetrieval=true&useSSL=false

  jpa:
    show-sql: true

jwtpassword:
  source: JWT_SECRET_KEY

  logging:
    level: debug
